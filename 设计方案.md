# Python Windows 定时任务调度系统 — 设计方案

> 版本: v1.0 | 日期: 2026-08-02

---

## 1. 项目概述

### 1.1 目标

开发一个 Windows 桌面定时任务调度系统，支持：

- **三种触发方式**：cron 表达式、固定间隔、一次性定时
- **指定运行目录**：每个任务可独立设置工作目录（cwd）
- **日志采集**：实时采集任务 stdout/stderr，按级别着色显示，持久化存储
- **开机自启**：系统启动后自动运行，最小化到系统托盘
- **打包分发**：PyInstaller 打包为单个 exe 或目录，无需安装 Python 环境

### 1.2 核心功能清单

| 功能 | 说明 |
|------|------|
| 任务管理 | 增删改查任务，支持启用/禁用 |
| 调度触发 | cron / interval / date 三种触发器 |
| 任务执行 | subprocess 调用外部命令/脚本，支持指定工作目录 |
| 日志采集 | 实时捕获 stdout/stderr，按级别着色，写入数据库 |
| 日志查看 | 实时日志面板 + 历史记录查询，支持搜索和过滤 |
| 系统托盘 | 最小化到托盘，后台持续调度 |
| 开机自启 | 注册表写入，开机后自动启动 |
| 单实例锁 | 防止重复打开多个实例 |
| 数据持久化 | SQLite 存储任务定义和运行日志，重启不丢失 |

---

## 2. 技术栈选型

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| GUI 框架 | **PySide6** | 6.6+ | 窗口界面、表格、日志面板、系统托盘 |
| 调度引擎 | **APScheduler** | 3.10+ | cron/interval/date 触发器，后台调度 |
| ORM | **SQLAlchemy** | 2.0+ | 数据库操作 |
| 数据库 | **SQLite** | 内置 | 本地数据存储 |
| 打包工具 | **PyInstaller** | 6.0+ | 打包为 Windows exe |
| 系统集成 | **winreg** | 内置 | 注册表读写（开机自启） |

### 2.1 为什么选 PySide6 而非 customtkinter

| 对比项 | customtkinter | PySide6 |
|--------|--------------|---------|
| 表格控件 | 无原生表格，需 Treeview 手搓 | QTableWidget 开箱即用，支持排序/编辑/右键 |
| 日志显示 | CTkTextbox 纯文本追加 | QPlainTextEdit 支持富文本着色、搜索高亮 |
| 线程通信 | 需手动管理 | QThread + 信号槽，天然线程安全 |
| 系统托盘 | 需额外引入 pystray | QSystemTrayIcon 内置 |
| 打包体积 | ~15MB | ~80MB（onedir 模式） |
| 学习曲线 | 低 | 中 |

**结论**：对于任务调度器这种需要富表格 + 实时日志的场景，PySide6 的控件能力远超 customtkinter，80MB 体积对桌面工具可接受。

### 2.2 依赖清单

```
PySide6>=6.6.0
APScheduler>=3.10.0
SQLAlchemy>=2.0.0
pyinstaller>=6.0.0
```

---

## 3. 系统架构

### 3.1 四层分层架构

```
┌─────────────────────────────────────────────────────┐
│  GUI 层 (PySide6)                                    │
│  MainWindow · TaskEditDialog · LogPanel · TrayIcon   │
├─────────────────────────────────────────────────────┤
│                    信号槽 (Signal/Slot)                │
├─────────────────────────────────────────────────────┤
│  核心服务层                                           │
│  SchedulerManager · TaskExecutor · LogCollector       │
├─────────────────────────────────────────────────────┤
│  数据层              │  系统集成层                     │
│  DatabaseManager     │  AutoStart · SingleInstance     │
│  SQLite + SQLAlchemy │                                 │
├─────────────────────────────────────────────────────┤
│  打包部署                                             │
│  PyInstaller --onedir · winreg 自启 · %APPDATA% 数据   │
└─────────────────────────────────────────────────────┘
```

### 3.2 模块职责

#### GUI 层

| 模块 | 职责 |
|------|------|
| `MainWindow` | 主窗口，包含任务列表表格、工具栏、菜单栏、状态栏 |
| `TaskEditDialog` | 任务编辑对话框，表单输入任务参数 |
| `LogPanel` | 日志面板，实时显示 + 历史查询，支持搜索/过滤/着色 |
| `TrayIcon` | 系统托盘图标，右键菜单，最小化到托盘 |

#### 核心服务层

| 模块 | 职责 |
|------|------|
| `SchedulerManager` | 封装 APScheduler，管理任务注册/移除/暂停/恢复 |
| `TaskExecutor` | 封装 subprocess，在 QThread 中执行任务并采集输出 |
| `LogCollector` | 日志收集器，解析级别、格式化、分发到 UI 和数据库 |

#### 数据层

| 模块 | 职责 |
|------|------|
| `DatabaseManager` | 数据库连接管理，表初始化，CRUD 操作 |

#### 系统集成层

| 模块 | 职责 |
|------|------|
| `AutoStart` | 注册表读写，控制开机自启 |
| `SingleInstance` | 单实例锁，防止重复启动 |

---

## 4. 数据库设计

### 4.1 表结构

#### tasks (任务定义表)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | 主键 |
| name | TEXT | NOT NULL | 任务名称 |
| description | TEXT | | 任务描述 |
| command | TEXT | NOT NULL | 执行命令 |
| work_dir | TEXT | | 工作目录 (cwd) |
| trigger_type | TEXT | NOT NULL | 触发类型: cron / interval / date |
| trigger_config | TEXT | NOT NULL | 触发配置 (JSON) |
| enabled | BOOLEAN | DEFAULT 1 | 是否启用 |
| max_instances | INTEGER | DEFAULT 1 | 最大并发实例数 |
| misfire_grace | INTEGER | DEFAULT 60 | 错过执行宽限期 (秒) |
| created_at | DATETIME | | 创建时间 |
| updated_at | DATETIME | | 更新时间 |
| last_run_at | DATETIME | | 上次运行时间 |
| next_run_at | DATETIME | | 下次运行时间 |

#### run_logs (运行日志表)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | 主键 |
| task_id | INTEGER | FK -> tasks.id | 关联任务 |
| started_at | DATETIME | NOT NULL | 开始时间 |
| finished_at | DATETIME | | 结束时间 |
| exit_code | INTEGER | | 退出码 |
| status | TEXT | | 状态: running / success / failed / timeout / killed |
| duration | REAL | | 运行时长 (秒) |
| log_content | TEXT | | 完整日志输出 |

#### app_config (配置表)

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| key | TEXT | PK | 配置键 |
| value | TEXT | | 配置值 |

预设配置项：`autostart`(开机自启)、`theme`(主题)、`log_retention_days`(日志保留天数)、`window_geometry`(窗口位置大小)。

### 4.2 trigger_config JSON 结构

```json
// cron 触发器
{"minute": "30", "hour": "2", "day_of_week": "mon-fri"}

// interval 触发器
{"seconds": 300}
// 或组合
{"minutes": 10, "seconds": 30}

// date 一次性触发
{"run_date": "2026-08-15T14:30:00"}
```

### 4.3 数据库文件位置

```
%APPDATA%\WinScheduler\tasks.db
```

使用 `os.environ['APPDATA']` 获取路径，确保打包后数据库不在 exe 目录下（避免升级时数据丢失）。

---

## 5. 界面设计

### 5.1 主窗口布局

```
┌──────────────────────────────────────────────────────────┐
│ 文件  任务  视图  帮助                          ─ □ ×    │
├──────────────────────────────────────────────────────────┤
│ [+ 新建] [编辑] [删除] | [▶ 运行] [■ 停止] | [刷新] [设置] │
├──────────────────────────────────────────────────────────┤
│ 任务名称    │ 类型     │ 调度表达式    │ 状态   │ 上次运行 │ 下次运行  │
├──────────────────────────────────────────────────────────┤
│ 数据库备份  │ cron     │ 0 2 * * *    │ 运行中 │ 02:00:15 │ 明日02:00 │
│ 日志清理    │ interval │ 每 30 分钟    │ 空闲   │ 13:20:00 │ 13:50:00 │
│ 报表生成    │ cron     │ 0 18 * * 5   │ 失败   │ 周五18:00 │ 下周五   │
│ 数据同步    │ date     │ 2026-08-15   │ 等待   │ —        │ 08-15    │
├──────────────────────────────────────────────────────────┤
│ ▼ 日志面板              [实时日志] [历史记录]  [搜索...] │
├──────────────────────────────────────────────────────────┤
│ [13:45:12] [INFO]  [数据库备份] 开始执行: mysqldump...    │
│ [13:45:13] [INFO]  [数据库备份] -- Dumping data: users    │
│ [13:45:15] [WARN]  [数据库备份] Table `sessions` is empty │
│ [13:45:17] [INFO]  [数据库备份] 备份完成，退出码: 0       │
├──────────────────────────────────────────────────────────┤
│ ● 调度器运行中  任务: 4个(3启用)  运行中: 1  自启: 已启用 │
└──────────────────────────────────────────────────────────┘
```

### 5.2 交互设计

| 操作 | 行为 |
|------|------|
| 双击任务行 | 打开编辑对话框 |
| 右键任务行 | 上下文菜单：运行/停止/编辑/复制/删除/查看日志 |
| 工具栏 ▶ 运行 | 手动触发选中任务 |
| 工具栏 ■ 停止 | 终止正在运行的任务 (process.terminate()) |
| 关闭窗口 | 最小化到系统托盘，不退出程序 |
| 托盘双击 | 显示主窗口 |
| 托盘右键 | 菜单：显示主窗口/暂停调度/退出 |

### 5.3 日志面板

- **实时日志**：QPlainTextEdit，深色背景，按级别着色
  - INFO → 灰色 `#B4B2A9`
  - WARN → 黄色 `#FAC775`
  - ERROR → 红色 `#E24B4A`
  - SUCCESS → 绿色 `#639922`
- **历史记录**：从 run_logs 表查询，支持按任务/时间/状态筛选
- **搜索框**：关键词高亮匹配
- **自动滚动**：新日志到达时自动滚动到底部（可锁定）

### 5.4 任务编辑对话框

```
┌── 任务编辑 ───────────────────────┐
│                                    │
│  任务名称: [__________________]    │
│  描述:     [__________________]    │
│                                    │
│  执行命令: [__________________]    │
│  工作目录: [__________] [浏览...]  │
│                                    │
│  触发方式: (○)cron (○)interval (○)date │
│                                    │
│  ── cron 配置 ──                   │
│  分钟:   [*]   小时: [2]           │
│  日:     [*]   月:   [*]           │
│  星期:   [mon-fri]                 │
│                                    │
│  ☑ 启用此任务                      │
│  最大并发: [1]    宽限期: [60]秒   │
│                                    │
│        [确定]    [取消]            │
└────────────────────────────────────┘
```

---

## 6. 核心模块设计

### 6.1 SchedulerManager

```python
class SchedulerManager:
    """封装 APScheduler，管理任务生命周期"""

    def __init__(self, db_manager: DatabaseManager):
        self.scheduler = BackgroundScheduler(
            jobstores={'default': SQLAlchemyJobStore(url=db_url)},
            executors={'default': ThreadPoolExecutor(max_workers=10)},
            job_defaults={'max_instances': 1, 'misfire_grace_time': 60}
        )
        self.task_executor = TaskExecutor()

    def start(self):
        """启动调度器，从数据库恢复所有启用的任务"""
        self.scheduler.start()
        self._reload_jobs()

    def add_job(self, task: Task):
        """注册新任务到调度器"""
        trigger = self._build_trigger(task.trigger_type, task.trigger_config)
        self.scheduler.add_job(
            func=self.task_executor.run,
            trigger=trigger,
            args=[task],
            id=str(task.id),
            replace_existing=True
        )

    def remove_job(self, task_id: int):
        """移除任务"""
        self.scheduler.remove_job(str(task_id))

    def pause_job(self, task_id: int):
        """暂停任务"""
        self.scheduler.pause_job(str(task_id))

    def resume_job(self, task_id: int):
        """恢复任务"""
        self.scheduler.resume_job(str(task_id))

    def _build_trigger(self, trigger_type: str, config: dict):
        """根据类型构建 APScheduler 触发器"""
        if trigger_type == 'cron':
            return CronTrigger(**config)
        elif trigger_type == 'interval':
            return IntervalTrigger(**config)
        elif trigger_type == 'date':
            return DateTrigger(**config)
```

### 6.2 TaskExecutor

```python
class TaskWorker(QThread):
    """在子线程中执行任务，实时采集日志"""

    log_signal = Signal(str, str)  # (task_name, log_line)
    finished_signal = Signal(int, int)  # (task_id, exit_code)

    def __init__(self, task: Task):
        super().__init__()
        self.task = task
        self.process = None

    def run(self):
        """QThread 入口：启动子进程并逐行读取输出"""
        started_at = datetime.now()
        log_buffer = []

        self.process = subprocess.Popen(
            self.task.command,
            cwd=self.task.work_dir,        # 指定工作目录
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,       # 合并 stderr 到 stdout
            text=True,
            bufsize=1,                     # 行缓冲
            encoding='utf-8',
            errors='replace',
            shell=True,                    # 支持 .bat / 管道 / 重定向
            creationflags=subprocess.CREATE_NO_WINDOW  # 隐藏控制台窗口
        )

        # 逐行读取输出，实时发射信号
        for line in self.process.stdout:
            line = line.rstrip('\n')
            log_buffer.append(line)
            self.log_signal.emit(self.task.name, line)

        self.process.wait()
        exit_code = self.process.returncode
        finished_at = datetime.now()

        # 发射完成信号
        self.finished_signal.emit(self.task.id, exit_code)

        # 写入数据库
        self._save_log(started_at, finished_at, exit_code, log_buffer)

    def stop(self):
        """终止任务"""
        if self.process:
            self.process.terminate()


class TaskExecutor:
    """任务执行器，管理 TaskWorker 实例"""

    def __init__(self):
        self.workers = {}  # task_id -> TaskWorker

    def run(self, task: Task):
        """执行任务（由 APScheduler 或手动触发调用）"""
        if task.id in self.workers and self.workers[task.id].isRunning():
            return  # 已在运行，跳过（受 max_instances 控制）

        worker = TaskWorker(task)
        self.workers[task.id] = worker
        worker.start()

    def stop(self, task_id: int):
        """停止任务"""
        if task_id in self.workers:
            self.workers[task_id].stop()
```

### 6.3 LogCollector

```python
class LogCollector:
    """日志收集器：解析级别、格式化、分发"""

    LEVEL_PATTERNS = {
        'ERROR': re.compile(r'error|exception|traceback|failed', re.IGNORECASE),
        'WARN':  re.compile(r'warn|warning|deprecated', re.IGNORECASE),
    }

    @classmethod
    def parse_level(cls, line: str, exit_code: int = None) -> str:
        """识别日志级别"""
        if exit_code is not None and exit_code == 0:
            return 'SUCCESS'
        if cls.LEVEL_PATTERNS['ERROR'].search(line):
            return 'ERROR'
        if cls.LEVEL_PATTERNS['WARN'].search(line):
            return 'WARN'
        return 'INFO'

    @classmethod
    def format_line(cls, timestamp: str, task_name: str, level: str, line: str) -> str:
        """格式化日志行"""
        return f"[{timestamp}] [{level:5s}] [{task_name}] {line}"

    @classmethod
    def get_color(cls, level: str) -> str:
        """获取级别对应颜色"""
        colors = {
            'ERROR':   '#E24B4A',
            'WARN':    '#FAC775',
            'INFO':    '#B4B2A9',
            'SUCCESS': '#639922',
        }
        return colors.get(level, '#B4B2A9')
```

### 6.4 AutoStart

```python
class AutoStart:
    """开机自启管理（注册表方式）"""

    REGISTRY_KEY = r"Software\Microsoft\Windows\CurrentVersion\Run"
    APP_NAME = "WinScheduler"

    @classmethod
    def enable(cls, exe_path: str):
        """启用开机自启"""
        key = winreg.OpenKey(
            winreg.HKEY_CURRENT_USER, cls.REGISTRY_KEY,
            0, winreg.KEY_SET_VALUE
        )
        winreg.SetValueEx(key, cls.APP_NAME, 0, winreg.REG_SZ, f'"{exe_path}" --minimized')
        winreg.CloseKey(key)

    @classmethod
    def disable(cls):
        """禁用开机自启"""
        key = winreg.OpenKey(
            winreg.HKEY_CURRENT_USER, cls.REGISTRY_KEY,
            0, winreg.KEY_SET_VALUE
        )
        winreg.DeleteValue(key, cls.APP_NAME)
        winreg.CloseKey(key)

    @classmethod
    def is_enabled(cls) -> bool:
        """检查是否已启用"""
        try:
            key = winreg.OpenKey(
                winreg.HKEY_CURRENT_USER, cls.REGISTRY_KEY,
                0, winreg.KEY_READ
            )
            winreg.QueryValueEx(key, cls.APP_NAME)
            winreg.CloseKey(key)
            return True
        except FileNotFoundError:
            return False
```

### 6.5 SingleInstance

```python
class SingleInstance:
    """单实例锁，防止重复启动"""

    def __init__(self):
        self.mutex = None

    def acquire(self) -> bool:
        """尝试获取锁，成功返回 True"""
        try:
            self.mutex = win32event.CreateMutex(None, False, "Global\\WinScheduler_Mutex")
            if win32api.GetLastError() == win32con.ERROR_ALREADY_EXISTS:
                return False
            return True
        except Exception:
            return False

    def release(self):
        """释放锁"""
        if self.mutex:
            win32api.CloseHandle(self.mutex)
```

> 备选方案：用 `socket.bind(('127.0.0.1', 47200))` 实现单实例锁，无需 pywin32 依赖。

---

## 7. 任务执行流程

### 7.1 线程模型

```
┌─ APScheduler 后台线程 ──┐   ┌─ Qt 主线程 (GUI) ──┐   ┌─ 数据持久化 ──┐
│                        │   │                    │   │              │
│  cron/interval 触发     │   │                    │   │              │
│       ↓                │   │                    │   │              │
│  SchedulerManager      │   │                    │   │              │
│  查库取任务定义          │   │                    │   │              │
│       ↓                │   │                    │   │              │
│  创建 TaskWorker        │   │                    │   │              │
│  (QThread 子线程)       │   │                    │   │              │
│       ↓                │   │                    │   │              │
│  subprocess.Popen      │   │                    │   │              │
│  cwd=work_dir          │   │                    │   │              │
│       ↓                │   │                    │   │              │
│  逐行读取 stdout        │───│→ 信号槽接收         │   │              │
│  for line in stdout    │   │  log_signal.emit() │   │              │
│       ↓                │   │       ↓            │   │              │
│                        │   │  LogPanel 追加显示  │───│→ LogCollector │
│                        │   │  着色 + 自动滚动    │   │  解析级别     │
│       ↓                │   │       ↓            │   │       ↓      │
│  进程结束               │   │  更新任务状态       │   │  追加 log_buf │
│  捕获 exit_code        │   │                    │   │       ↓      │
│       ↓                │   │                    │   │  SQLite      │
│  写入 run_logs 记录     │───────────────────────────│→ run_logs 表  │
│                        │   │                    │   │              │
└────────────────────────┘   └────────────────────┘   └──────────────┘
```

### 7.2 关键设计点

1. **线程安全**：subprocess 在 QThread 中执行，日志通过 `pyqtSignal` 传递到主线程更新 UI，Qt 的信号槽机制保证线程安全。

2. **日志缓冲**：LogCollector 在内存中缓冲日志行，任务结束时一次性写入 `run_logs.log_content`，避免高频数据库写入。

3. **CREATE_NO_WINDOW**：subprocess 添加 `creationflags=subprocess.CREATE_NO_WINDOW`，避免弹出黑色控制台窗口。

4. **手动触发**：工具栏"运行"按钮和右键菜单"运行"都走相同的 TaskWorker 路径，与定时触发一致。

5. **日志级别识别**：通过正则匹配 stdout 内容中的 error/warning 等关键词判定级别，退出码 0 判定为 SUCCESS。

---

## 8. 开机自启方案

### 8.1 注册表方式（推荐）

写入 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`：
- 无需管理员权限
- 用户级自启，登录后自动运行
- `--minimized` 参数启动后直接最小化到托盘

### 8.2 命令行参数处理

```python
parser = argparse.ArgumentParser()
parser.add_argument('--minimized', action='store_true', help='启动后最小化到托盘')
args = parser.parse_args()

if args.minimized:
    # 不显示主窗口，直接显示托盘图标
    tray_icon.show()
else:
    main_window.show()
```

---

## 9. 打包方案

### 9.1 PyInstaller 命令

```bash
pyinstaller \
    --noconsole \
    --onedir \
    --name WinScheduler \
    --icon assets/icon.ico \
    --collect-all PySide6 \
    --hidden-import apscheduler.triggers.cron \
    --hidden-import apscheduler.triggers.interval \
    --hidden-import apscheduler.triggers.date \
    --hidden-import apscheduler.jobstores.sqlalchemy \
    --add-data "assets;assets" \
    main.py
```

### 9.2 注意事项

| 问题 | 解决方案 |
|------|---------|
| APScheduler 隐式导入 | 添加 `--hidden-import` 指定触发器和 JobStore 模块 |
| PySide6 Qt 插件缺失 | 使用 `--collect-all PySide6` 自动收集 |
| 数据库路径 | 使用 `%APPDATA%\WinScheduler\` 而非 exe 同目录 |
| 控制台黑框 | `--noconsole` / `--windowed` 隐藏控制台 |
| 杀毒软件误报 | 使用 `--onedir`（比 `--onefile` 误报率低），或用 Nuitka 编译 |

### 9.3 目录结构

```
WinScheduler/
├── WinScheduler.exe        # 主程序
├── _internal/               # PySide6 + 依赖
│   ├── PySide6/
│   ├── apscheduler/
│   ├── sqlalchemy/
│   └── ...
└── assets/
    └── icon.ico
```

数据文件位置（不打包，运行时创建）：
```
%APPDATA%\WinScheduler\
├── tasks.db                 # 数据库
└── config.json              # 配置文件
```

---

## 10. 文件结构

```
WinScheduler/
├── main.py                    # 程序入口
├── config.py                  # 全局配置 (路径、常量)
├── database/
│   ├── __init__.py
│   ├── models.py              # SQLAlchemy 模型定义
│   └── manager.py             # DatabaseManager
├── core/
│   ├── __init__.py
│   ├── scheduler_manager.py   # SchedulerManager
│   ├── task_executor.py       # TaskWorker + TaskExecutor
│   └── log_collector.py       # LogCollector
├── ui/
│   ├── __init__.py
│   ├── main_window.py         # MainWindow
│   ├── task_edit_dialog.py    # TaskEditDialog
│   ├── log_panel.py           # LogPanel
│   └── tray_icon.py           # TrayIcon
├── system/
│   ├── __init__.py
│   ├── autostart.py           # AutoStart
│   └── single_instance.py     # SingleInstance
├── assets/
│   └── icon.ico
├── build.spec                 # PyInstaller spec 文件
├── requirements.txt
└── README.md
```

---

## 11. 开发计划

### 阶段 1：MVP 核心 (基础可用)

| 任务 | 预估 |
|------|------|
| 项目骨架 + 数据库模型 | 搭建文件结构，定义 SQLAlchemy 模型，初始化数据库 |
| SchedulerManager | 封装 APScheduler，实现任务注册/移除/暂停 |
| TaskExecutor | QThread + subprocess 执行任务，采集 stdout |
| MainWindow + 任务列表 | QTableWidget 显示任务，工具栏增删改查 |
| TaskEditDialog | 表单编辑任务，支持 cron/interval/date 配置 |
| 基础日志面板 | QPlainTextEdit 实时显示日志 |

### 阶段 2：完善功能

| 任务 | 预估 |
|------|------|
| 日志着色 + 搜索 | LogCollector 级别解析，QPlainTextEdit 着色 |
| 历史日志查询 | 从 run_logs 表查询，支持按任务/时间/状态筛选 |
| 系统托盘 | QSystemTrayIcon，最小化到托盘，右键菜单 |
| 开机自启 | AutoStart 注册表读写，设置界面开关 |
| 单实例锁 | 防止重复启动 |
| 关闭窗口最小化 | closeEvent 拦截，最小化到托盘 |

### 阶段 3：打包发布

| 任务 | 预估 |
|------|------|
| PyInstaller 打包 | 编写 spec 文件，处理 hidden imports |
| 图标 + 资源 | 制作应用图标，打包 assets |
| 测试验证 | 在干净 Windows 环境验证运行 |
| 日志轮转 | 按天数/条数自动清理旧日志 |

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| PyInstaller 打包后 APScheduler 找不到触发器 | 任务无法触发 | 添加 `--hidden-import` |
| subprocess 在无窗口模式下 .bat 脚本异常 | 任务执行失败 | `shell=True` + `CREATE_NO_WINDOW` |
| SQLite 并发写入冲突 | 日志丢失 | 使用 WAL 模式，写入加锁 |
| 大量日志导致数据库膨胀 | 性能下降 | 日志轮转 + log_content 压缩存储 |
| 杀毒软件误报 --onefile | 用户无法使用 | 使用 --onedir 或 Nuitka |
| 时区问题 | 任务触发时间错误 | APScheduler 使用本地时区，统一 datetime 处理 |
